# 发给服务器新会话的任务

请在我提供的服务器 2-step 仓库中，实现设计包中的 MHINet。先定位本设计包并读 START_HERE.md，再读 docs/01、03、05、06、02、07、08 及 configs/，以训练协议 v1.2 为当前要求。history/ 不作为实施指令。设计包、仓库、数据和权重路径用我提供的实际位置；如果信息可从服务器现有文件确定，请自行定位并记录。

目标是 D8/D4/D2/D1 各两轮的 H 迭代，暂不加入 Planar。保留现有 Stage1 和 VGG/DeDoDe 权重接入；共享 DINOv3/MVT 只计算一次。DINO 始终冻结，MVT、VGG、DeDoDe 与新增层支持联合训练；Stage1 head 参数默认冻结但其输入梯度保留，联合阶段 H0 不 detach。默认八轮 proposal 四角 L1，FGO 和额外损失不开启。

先检查仓库当前分支、未提交修改及与设计审计提交的差异，保留已有工作。在独立的 experiments/mhinet_v1 接入，优先复用兼容的原函数，不改变旧实验行为。结构配置中已有完整层定义，不临时替换成 GRU、稀疏关键点、dense H solver 或新 backbone。

按 docs/06 的 P0–P6 逐步实现，并把实际文件、命令、资源路径、环境版本、checkpoint hash 和验证结果写入 implementation_log.md。先完成环境/旧输出对齐、几何和相关性 reference、参数与 zero-init 检查、梯度审计、单尺度和八轮 tiny-overfit；通过后才做 E00/E01 训练。出现不通过时定位并修复，避免跳到更复杂损失掩盖问题。

特别检查旧 inference_mode/no_grad/train() 包装。冻结 head 不等于用 no_grad 包住 head。禁止在八轮之间 detach H/T；非法线性系统须在可微求解前隔离，不能计算奇异逆/除法后只用 where 隐藏 NaN。FC2 零初始化的第一步前层梯度可为零，需第二步之后和两条 MVT 梯度分支分别核验。

主线通过后按实验表开展 E02–E05 等预算解冻比较，再做尺度/迭代/重采样消融。先一个种子筛查，再对选定主配置和关键对照做三个种子。数据按母图/地理区域分组，test 不用于选择配置。所有结果同时记录 H0、每轮 H、最终 H 的几何误差、失败率、显存和延迟。

最终交付可复现训练/验证入口、最小运行配置、测试与 tiny-overfit 证据、实验记录和下一步建议。不要将“代码写完”标记为“模型验证成功”。如果缺失的是必须的真实权重、数据或标签，完成可独立进行的模块和测试，再准确列出阻塞资源；不要编造路径或训练结果。
